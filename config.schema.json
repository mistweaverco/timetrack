{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://timetrack.mwco.app/config.schema.json",
  "title": "Timetrack configuration",
  "description": "Configuration file for the Timetrack desktop application.",
  "type": "object",
  "properties": {
    "databases": {
      "description": "Optional list or map of named database file paths. Names must be unique.",
      "oneOf": [
        {
          "type": "array",
          "items": {
            "type": "object",
            "minProperties": 1,
            "maxProperties": 1,
            "additionalProperties": {
              "type": "string",
              "description": "Absolute path to a SQLite database file."
            }
          }
        },
        {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "description": "Absolute path to a SQLite database file."
          }
        }
      ]
    }
  },
  "additionalProperties": false
}
